[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "gecko-hide-generator"
version = "0.1.0"
description = "Deterministic CadQuery generator for printable gecko hides"
requires-python = ">=3.11"
dependencies = [
    "cadquery>=2.5,<2.7",
    "numpy>=1.26",
    "trimesh>=4.4",
    "matplotlib>=3.8",
    "bokeh>=3.5",
    "scipy>=1.13",
    "shapely>=2.0",
    "fastapi>=0.104,<1",
    "uvicorn>=0.24,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
